# Install as /etc/systemd/system/agy-net.service.d/dns.conf when reusing an
# already connected amn0 without copying an AWG configuration onto the host.
[Service]
Environment=AGY_NET_DNS=1.1.1.1,1.0.0.1
